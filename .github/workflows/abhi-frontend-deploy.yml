name: Build and Deploy Angular App to Azure VM

on:
   push:
     branches:
       - RV_abhi_frontend_deploy
      # - Dev
      # - release
  # workflow_dispatch:
  #   inputs:
  #     environment:
  #       description: 'Deployment environment'
  #       required: true
  #       default: 'abhi_dev'
  #       type: choice
  #       options:
  #         - abhi_dev
  #         - abhi_uat
  #         - abhi_prod

env:
  SONAR_PROJECT_KEY: "frontend-sonar-project"
  DEPLOY_BASE_DIR: "C:/ABHI_DATA/abhi_deployments/frontend_deployments"
  WEB_ROOT: "C:/UnifiedPortal/ABHI"
  APP_POOL_NAME: "ABHIUP"
  SITE_NAME: "ABHIUP"
  BINDING_HOST: "usp.monocept.ai"
  PORT: 443

jobs:
  build:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        fetch-depth: 10

    # - name: Set up Node.js
    #   uses: actions/setup-node@v3
    #   with:
    #     node-version: '18.20.3'

    
    # - name: Cache Node modules
    #   uses: actions/cache@v3
    #   with:
    #     path: |
    #       ~/.npm
    #       ~/.cache
    #     key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
    #     restore-keys: |
    #       ${{ runner.os }}-node-

    - name: Print working directory and list its contents
      run: |
        echo "Current working directory is:"
        pwd  # Print the current working directory
        echo "Listing the contents of the current directory:"
        ls -la  # List the contents of the current working directory
  
    - name: List MozartUI-Dev directory contents
      run: |
        echo "Listing the contents of MozartUI-Dev directory:"
        ls -la MozartUI-Dev  # List the contents of the MozartUI-Dev directory (if it exists)

    # - name: Install dependencies
    #   run: |
    #     cd MozartUI-Dev
    #     npm install --legacy-peer-deps

    # - name: Install dependencies
    #   run: |
    #     npm install --legacy-peer-deps

    # - name: Clean previous build artifacts
    #   run: |
    #     rm -rf ./dist/*
      
    # - name: Build the project
    #   run: |
    #     npm run build --prod

    # - name: List Build Artifacts
    #   run: ls -la ./dist/

    # - name: List contents in the current directory
    #   run: |
    #     pwd
    #     ls -la 
    #     ls -la MozartUI-Dev
      
    # - name: Upload build artifacts
    #   uses: actions/upload-artifact@v3
    #   with:
    #     name: angular-artifacts
    #     path: ./dist/*

    # - name: Debug:List files after downloading artifacts
    #   run: ls -l ./dist/

    # - name: SonarQube Scan
    #   uses: sonarsource/sonarqube-scan-action@v3.0.0
    #   with:
    #     projectBaseDir: .
    #     args: |
    #       -Dsonar.projectKey=${{ env.SONAR_PROJECT_KEY }}
    #       -Dsonar.sources=./src
    #   env:
    #     SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    #     SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

    # - name: Check SonarQube Quality Gate
    #   run: |
    #     status=$(curl -s -u ${{ secrets.SONAR_TOKEN }}: \
    #       "${{ secrets.SONAR_HOST_URL }}/api/qualitygates/project_status?projectKey=${{ env.SONAR_PROJECT_KEY }}" \
    #       | jq -r .projectStatus.status)
    #     echo "Quality Gate status: $status"
    #     if [ "$status" != "OK" ]; then
    #       echo "Quality Gate failed: $status"
    #       exit 1
    #     fi

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment: 
      name: ${{ github.ref_name }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        fetch-depth: 10
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Set up SSH
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa

    - name: Add VM to known hosts
      run: ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

    # Generate the single version at the workflow level
    - name: Generate Deployment Version
      id: version
      run: |
        # Generate version using current date and time (IST)
        #CURRENT_DATE=$(date -d 'TZ="Asia/Kolkata" now' +"%Y%m%d-%H%M%S")
        CURRENT_DATE=$(TZ="Asia/Kolkata" date +"%Y%m%d-%H%M%S")
        TAGGED_VERSION="v${CURRENT_DATE}"
        echo "Deployment version: $TAGGED_VERSION"
        # Set the version as an output
        echo "::set-output name=version::$TAGGED_VERSION"
        # Also export it as an environment variable for other steps
        echo "TAGGED_VERSION=$TAGGED_VERSION" >> $GITHUB_ENV
  
    - name: Debug:Show generated version
      run: |
        echo "Generated version: $TAGGED_VERSION"
  
    # - name: Create deployment directory on VM with version
    #   run: |
    #     ssh -i ~/.ssh/id_rsa -p 22 ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} powershell -Command "\$DeployDir = '${{ env.DEPLOY_BASE_DIR }}\\$TAGGED_VERSION'; try { if (-Not (Test-Path -Path \$DeployDir)) { Write-Output \"Creating deployment directory: \$DeployDir\"; New-Item -Path \$DeployDir -ItemType Directory; } else { Write-Output \"Deployment directory already exists: \$DeployDir\"; } } catch { Write-Output \"Error while creating deployment directory: \$DeployDir\"; Write-Output \$_.Exception.Message; exit 1; }"

    # - name: Manage Previous Deployments (Keep Last 3)
    #   run: ssh -i ~/.ssh/id_rsa -p 22 ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "powershell -Command \"\$deploymentDir = '${{ env.DEPLOY_BASE_DIR }}'; \$deployments = Get-ChildItem -Path \$deploymentDir | Sort-Object LastWriteTime -Descending; if (\$deployments.Count -gt 3) { \$deployments | Select-Object -Skip 3 | Remove-Item -Recurse -Force }\""
    
    # - name: Create directory for artifacts
    #   run: mkdir -p ./my-artifacts
      
    # - name: Download build artifacts
    #   uses: actions/download-artifact@v3
    #   with:
    #     name: angular-artifacts
    #     path: ./my-artifacts
    
    # - name: Debug:List files after downloading artifacts
    #   run: ls -l ./my-artifacts

    # - name: Upload artifact to VM with versioning
    #   run: scp -i ~/.ssh/id_rsa -P 22 -r ./my-artifacts/angular-dev/* ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:$DEPLOY_BASE_DIR/$TAGGED_VERSION/
#######################
    # - name: Create or Update Application Pool
    #   run: |
    #     ssh -i ~/.ssh/id_rsa -p 22 ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} powershell -Command "$AppPoolName='${{ env.APP_POOL_NAME }}';try {if (Test-Path 'IIS:\\AppPools\\$AppPoolName') {Write-Output \"Application Pool '$AppPoolName' already exists. Skipping creation.\";} else {Write-Output \"Creating Application Pool: $AppPoolName\";New-WebAppPool -Name $AppPoolName;Set-ItemProperty IIS:\\AppPools\\$AppPoolName -Name managedRuntimeVersion -Value 'v4.0';Set-ItemProperty IIS:\\AppPools\\$AppPoolName -Name processModel.identityType -Value ApplicationPoolIdentity;}} catch {Write-Output \"Error while creating Application Pool: $AppPoolName\";Write-Output $_.Exception.Message;exit 1;}"
########################    
    - name: Upload deploy.ps1 to VM
      run: |
        scp -i ~/.ssh/id_rsa -P 22 ./script/frontend_deploy.ps1 ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{env.DEPLOY_BASE_DIR}}/frontend_deploy.ps1
    
    - name: Execute deploy.ps1 script on VM
      run: |
        ssh -i ~/.ssh/id_rsa -p 22 ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} powershell -ExecutionPolicy Bypass -File /path/to/destination/deploy.ps1 `
          -AppPoolName "${{ env.APP_POOL_NAME }}" `
          -SiteName "${{ env.SITE_NAME }}" `
          -WebRoot "${{ env.WEB_ROOT }}" `
          -DeployBaseDir "${{ env.DEPLOY_BASE_DIR }}" `
          -TaggedVersion "${{ env.TAGGED_VERSION }}"
#######################################################################################
    # - name: Create or Update Application Pool
    #   run: |
    #     ssh -i ~/.ssh/id_rsa -p 22 ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} powershell -Command "\$AppPoolName = '${{ env.APP_POOL_NAME }}'; try { if (Test-Path 'IIS:\\AppPools\\\$AppPoolName') { Write-Output \"Application Pool '\$AppPoolName' already exists. Skipping creation.\"; } else { Write-Output \"Creating Application Pool: \$AppPoolName\"; New-WebAppPool -Name \$AppPoolName; Set-ItemProperty IIS:\\AppPools\\\$AppPoolName -Name managedRuntimeVersion -Value 'v4.0'; Set-ItemProperty IIS:\\AppPools\\\$AppPoolName -Name processModel.identityType -Value ApplicationPoolIdentity; } } catch { Write-Output \"Error while creating Application Pool: \$AppPoolName\"; Write-Output \$_.Exception.Message; exit 1; }"

    # - name: Stop IIS Website if Exists
    #   run: ssh -i ~/.ssh/id_rsa -p 22 ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "powershell -Command \"if (Get-WebSite -Name '${{ env.SITE_NAME }}' -ErrorAction SilentlyContinue) { Stop-WebSite -Name '${{ env.SITE_NAME }}' } else { Write-Output 'Website not found' }\""
    
    # - name: Remove Existing Website if Exists
    #   run: ssh -i ~/.ssh/id_rsa -p 22 ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "powershell -Command \"if (Get-WebSite -Name '${{ env.SITE_NAME }}' -ErrorAction SilentlyContinue) { Remove-WebSite -Name '${{ env.SITE_NAME }}' } else { Write-Output 'Website not found' }\""
  
    # - name: Ensure frontend Directory Exists ( Web Root )
    #   run: ssh -i ~/.ssh/id_rsa -p 22 ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "powershell -Command \"if (-Not (Test-Path -Path '${{ env.WEB_ROOT }}')) { New-Item -Path '${{ env.WEB_ROOT }}' -ItemType Directory }\""
  
    # - name: Clean up the frontend Directory
    #   run: ssh -i ~/.ssh/id_rsa -p 22 ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "powershell -Command \"Remove-Item -Path '${{ env.WEB_ROOT }}\\*' -Recurse -Force\""
  
    # - name: Copy Latest Build to IIS web root directory
    #   run: ssh -i ~/.ssh/id_rsa -p 22 ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "powershell -Command \"Copy-Item -Path '$DEPLOY_BASE_DIR/$TAGGED_VERSION/*' -Destination '${{ env.WEB_ROOT }}' -Recurse -Force\""
    

    # - name: Deploy New IIS Site
    #   run: |
    #     ssh -i ~/.ssh/id_rsa ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} powershell -Command "New-WebSite -Name '${{ env.SITE_NAME }}' -PhysicalPath '${{ env.DEPLOY_BASE_DIR }}\\${{ env.TAGGED_VERSION }}' -ApplicationPool '${{ env.APP_POOL_NAME }}' -Port ${{ env.PORT }} -HostHeader '${{ env.BINDING_HOST }}'"

    # - name: Bind SSL Certificate (Optional)
    #   run: |
    #     ssh -i ~/.ssh/id_rsa ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} powershell -Command "
    #       New-WebBinding -Name '${{ env.SITE_NAME }}' -Protocol 'https' -Port ${{ env.PORT }} `
    #         -HostHeader '${{ env.BINDING_HOST }}' -CertificateThumbprint '<CERTIFICATE_THUMBPRINT>' `
    #         -CertificateStoreName 'My'
    #     "

    # - name: Import SSL Certificate and Bind to IIS
    #   run: |
    #     ssh -i ~/.ssh/id_rsa -p 22 ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} powershell -Command "
    #       \$SSLCertPath = 'C:\\Users\\ABHI\\Desktop\\monolensssl2024.pfx';  # Path where the certificate will be stored
    #       \$SSLCertPassword = '${{ secrets.SSL_CERT_PASSWORD }}';        # Certificate password (store it in secrets)
    #       \$SiteName = '${{ env.SITE_NAME }}';                    # Website name
    #       \$WebRoot = '${{ env.WEB_ROOT }}';                      # Web root path
    #       \$AppPoolName = '${{ env.APP_POOL_NAME }}';             # App pool name

    #       # 1. Ensure the certificate path exists on the server (optional if it's already pre-stored)
    #       if (-Not (Test-Path -Path \$SSLCertPath)) {
    #         Write-Error 'SSL certificate file does not exist at the specified path.';
    #         exit 1;
    #       }

    #       # 2. Upload the certificate file
    #       # Write-Output 'Uploading SSL certificate file...';
    #       # scp -i ~/.ssh/id_rsa -P 22 ./path-to-your-cert/your-certificate.pfx ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:C:\\certificates\\your-certificate.pfx

    #       # 3. Import the certificate into the local machine store
    #       Write-Output 'Importing SSL Certificate...';
    #       \$cert = Import-PfxCertificate -FilePath \$SSLCertPath -CertStoreLocation Cert:\\LocalMachine\\My -Password (ConvertTo-SecureString -String \$SSLCertPassword -AsPlainText -Force);

    #       # 4. Check if the website exists in IIS
    #       # if (-Not (Get-WebSite -Name \$SiteName -ErrorAction SilentlyContinue)) {
    #       #   Write-Output 'Creating IIS Website...';
    #       #   New-WebSite -Name \$SiteName -PhysicalPath \$WebRoot -Port 80 -ApplicationPool \$AppPoolName;
    #       # }

    #       # 5. Configure SSL Binding
    #       Write-Output 'Configuring SSL binding...';
    #       \$CertThumbprint = \$cert.Thumbprint;
    #       New-WebBinding -Name \$SiteName -Protocol https -Port 443 -CertificateThumbprint \$CertThumbprint -CertificateStoreName My;

    #       # 6. Verify the SSL Binding
    #       Write-Output 'SSL Binding Complete!';
    #       Get-WebBinding -Name \$SiteName | Where-Object { \$_.protocol -eq 'https' }
    #     "
################
    # - name: Start IIS Site
 ###   #   run: |
    #     ssh -i ~/.ssh/id_rsa ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} powershell -Command "Start-WebSite -Name '${{ env.SITE_NAME }}'"
#####################
    # - name: Copy Latest Build to IIS web root directory
    #   run: ssh -i ~/.ssh/id_rsa -p 22 ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "powershell -Command \"Copy-Item -Path '$DEPLOY_BASE_DIR/$TAGGED_VERSION/*' -Destination '$WEB_ROOT' -Recurse -Force\""
    
    # - name: Deploy application on IIS
    #   run: ssh -i ~/.ssh/id_rsa -p 22 ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "powershell -Command \"New-WebSite -Name 'AngularApp' -Port $PORT -PhysicalPath ('${{ env.WEB_ROOT }}' -replace '/', '\\') -ApplicationPool 'ABHIUP'\""

    # - name: Set environment variable based on branch
    #   run: |
    #     branch_name="${GITHUB_REF#refs/heads/}" # Strip out 'refs/heads/' from branch name
    #     environment=${branch_name}
    #     if [ "$environment" == "main" ]; then
    #       environment="prod"
    #     fi
    #     echo "ENVIRONMENT=$environment" >> $GITHUB_ENV

    # - name: Tag the commit
    #   if: ${{ success() }}  # Only tag if the workflow is successful
    #   run: |
    #     TAG="$ENVIRONMENT-$TAGGED_VERSION"
    #     echo "Tag: $TAG"  # Debugging the tag value
    #     git tag $TAG
    #     git push origin $TAG
    