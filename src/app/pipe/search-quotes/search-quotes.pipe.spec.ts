import { SearchQuotesPipe } from './search-quotes.pipe';

describe('SearchQuotesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchQuotesPipe();
    expect(pipe).toBeTruthy();
  });
});
